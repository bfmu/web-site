---
import Layout from '@layouts/Layout.astro';
import AdminThemeSwitch from '@components/admin/AdminThemeSwitch.svelte';
import { getUser } from '../../lib/auth';

interface Props {
  title?: string;
}

const { title = 'Admin' } = Astro.props;

// En SSR, no podemos acceder a localStorage, así que getUser() retornará null
// No hacer redirect aquí, el middleware y el cliente se encargan de la protección
// Solo obtener el usuario si está disponible (en el cliente)
let user = null;
if (typeof window !== 'undefined') {
  user = getUser();
}
---

<Layout title={title}>
  <div class="min-h-screen bg-gray-50 dark:bg-gray-900">
    <!-- Sidebar -->
    <aside
      id="admin-sidebar"
      class="fixed left-0 top-0 z-40 h-screen w-64 -translate-x-full border-r border-gray-200 bg-white transition-transform dark:border-gray-700 dark:bg-gray-800 lg:translate-x-0"
    >
      <div class="flex h-full flex-col">
        <!-- Logo/Title -->
        <div class="flex h-16 items-center border-b border-gray-200 px-6 dark:border-gray-700">
          <h1 class="text-xl font-bold text-gray-900 dark:text-white">Admin Panel</h1>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 space-y-1 px-3 py-4">
          <a
            href="/admin"
            class="flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 transition-colors hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-700"
            class:list={[
              {
                'bg-gray-100 dark:bg-gray-700': Astro.url.pathname === '/admin',
              },
            ]}
          >
            <svg
              class="h-5 w-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
              />
            </svg>
            <span>Dashboard</span>
          </a>

          <a
            href="/admin/posts"
            class="flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 transition-colors hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-700"
            class:list={[
              {
                'bg-gray-100 dark:bg-gray-700': Astro.url.pathname.startsWith('/admin/posts'),
              },
            ]}
          >
            <svg
              class="h-5 w-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
              />
            </svg>
            <span>Posts</span>
          </a>

          <a
            href="/admin/media"
            class="flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 transition-colors hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-700"
            class:list={[
              {
                'bg-gray-100 dark:bg-gray-700': Astro.url.pathname.startsWith('/admin/media'),
              },
            ]}
          >
            <svg
              class="h-5 w-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"
              />
            </svg>
            <span>Media</span>
          </a>

          <a
            href="/admin/albums"
            class="flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 transition-colors hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-700"
            class:list={[
              {
                'bg-gray-100 dark:bg-gray-700': Astro.url.pathname.startsWith('/admin/albums'),
              },
            ]}
          >
            <svg
              class="h-5 w-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"
              />
            </svg>
            <span>Álbumes</span>
          </a>

          <a
            href="/admin/profile"
            class="flex items-center gap-3 rounded-lg px-3 py-2 text-gray-700 transition-colors hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-700"
            class:list={[
              {
                'bg-gray-100 dark:bg-gray-700': Astro.url.pathname === '/admin/profile',
              },
            ]}
          >
            <svg
              class="h-5 w-5"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
              />
            </svg>
            <span>Mi Perfil</span>
          </a>
          
        </nav>

        <!-- User section -->
        <div class="border-t border-gray-200 p-4 dark:border-gray-700">
          <div id="user-info" class="flex items-center gap-3">
            <div class="h-8 w-8 rounded-full bg-gray-300 dark:bg-gray-600"></div>
            <div class="flex-1 min-w-0">
              <p class="truncate text-sm font-medium text-gray-900 dark:text-white">
                Cargando...
              </p>
              <p class="truncate text-xs text-gray-500 dark:text-gray-400">
                ...
              </p>
            </div>
          </div>
          <button
            id="logout-btn"
            class="mt-3 w-full rounded-lg bg-red-600 px-3 py-2 text-sm font-medium text-white transition-colors hover:bg-red-700"
          >
            Cerrar Sesión
          </button>
        </div>
      </div>
    </aside>

    <!-- Mobile sidebar overlay -->
    <div
      id="sidebar-overlay"
      class="fixed inset-0 z-30 bg-gray-900/50 opacity-0 transition-opacity lg:hidden"
      style="pointer-events: none;"
    ></div>

    <!-- Main content -->
    <div class="lg:pl-64">
      <!-- Top bar -->
      <header class="sticky top-0 z-30 flex h-16 items-center gap-4 border-b border-gray-200 bg-white px-4 shadow-sm dark:border-gray-700 dark:bg-gray-800 lg:px-6 overflow-visible">
        <button
          id="sidebar-toggle"
          class="lg:hidden"
          aria-label="Toggle sidebar"
        >
          <svg
            class="h-6 w-6 text-gray-600 dark:text-gray-300"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"
            />
          </svg>
        </button>

        <div class="flex-1">
          <h2 class="text-lg font-semibold text-gray-900 dark:text-white">
            {title}
          </h2>
        </div>

        <div class="flex items-center gap-2 overflow-visible">
          <div class="flex items-center">
            <AdminThemeSwitch client:load></AdminThemeSwitch>
          </div>
          <a
            id="view-site-link"
            href="/admin/view-site"
            class="hidden rounded-lg px-3 py-2 text-sm font-medium text-gray-700 transition-colors hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-700 flex items-center h-11"
            title="Ver sitio dentro del dashboard"
          >
            Ver Sitio
          </a>
          <a
            id="go-to-site-link"
            href="/"
            class="hidden rounded-lg px-3 py-2 text-sm font-medium text-gray-700 transition-colors hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-700 flex items-center gap-1.5 h-11"
            title="Ir al sitio principal"
          >
            <svg
              class="h-4 w-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
              />
            </svg>
            Ir al Sitio
          </a>
        </div>
      </header>

      <!-- Page content -->
      <main class="p-4 lg:p-6" class:list={[
        {
          'p-0': Astro.url.pathname === '/admin/view-site',
        },
      ]}>
        <slot />
      </main>
    </div>
  </div>
</Layout>

<script>
  import { logout, getUser, isAuthenticated, isAdmin } from '../../lib/auth';

  // Verificar autenticación en el cliente y redirigir si es necesario
  if (!isAuthenticated()) {
    window.location.href = '/admin/login';
  }

  // Verificar que el usuario tenga permisos (solo admin y editor pueden acceder)
  const currentUser = getUser();
  if (currentUser && !isAdmin() && currentUser.role !== 'editor') {
    // Usuarios normales no pueden acceder al admin
    alert('No tienes permisos para acceder al panel de administración.');
    window.location.href = '/';
  }

  // Mostrar "Ver Sitio" e "Ir al Sitio" solo para administradores (solo en el header)
  const viewSiteLink = document.getElementById('view-site-link');
  const goToSiteLink = document.getElementById('go-to-site-link');
  
  if (currentUser && isAdmin()) {
    if (viewSiteLink) {
      viewSiteLink.classList.remove('hidden');
    }
    if (goToSiteLink) {
      goToSiteLink.classList.remove('hidden');
    }
  }

  // Cargar información del usuario en el cliente
  const user = getUser();
  const userInfo = document.getElementById('user-info');
  if (user && userInfo) {
    // Construir URL completa del avatar
    function getAvatarUrl(avatar: string | undefined): string {
      if (!avatar || avatar === '/default-avatar.svg') {
        return '/default-avatar.svg';
      }
      if (avatar.startsWith('http')) {
        return avatar;
      }
      if (avatar.startsWith('/uploads/')) {
        const apiUrl = import.meta.env.PUBLIC_API_URL || 'http://localhost:4000/';
        return `${apiUrl.replace(/\/$/, '')}${avatar}`;
      }
      return avatar;
    }

    const avatarUrl = getAvatarUrl(user.avatar);
    userInfo.innerHTML = `
      <img 
        src="${avatarUrl}" 
        alt="${user.name}" 
        class="h-8 w-8 rounded-full object-cover border border-gray-200 dark:border-gray-700"
        onerror="this.src='/default-avatar.svg'"
      />
      <div class="flex-1 min-w-0">
        <p class="truncate text-sm font-medium text-gray-900 dark:text-white">
          ${user.name}
        </p>
        <p class="truncate text-xs text-gray-500 dark:text-gray-400">
          ${user.email}
        </p>
      </div>
    `;
  }

  // Toggle sidebar en mobile
  const sidebar = document.getElementById('admin-sidebar');
  const overlay = document.getElementById('sidebar-overlay');
  const toggleBtn = document.getElementById('sidebar-toggle');

  function openSidebar() {
    sidebar?.classList.remove('-translate-x-full');
    overlay?.classList.remove('opacity-0');
    overlay?.style.setProperty('pointer-events', 'auto');
  }

  function closeSidebar() {
    sidebar?.classList.add('-translate-x-full');
    overlay?.classList.add('opacity-0');
    overlay?.style.setProperty('pointer-events', 'none');
  }

  toggleBtn?.addEventListener('click', () => {
    if (sidebar?.classList.contains('-translate-x-full')) {
      openSidebar();
    } else {
      closeSidebar();
    }
  });

  overlay?.addEventListener('click', closeSidebar);

  // Logout
  const logoutBtn = document.getElementById('logout-btn');
  logoutBtn?.addEventListener('click', async () => {
    if (confirm('¿Estás seguro de que quieres cerrar sesión?')) {
      await logout();
      window.location.href = '/admin/login';
    }
  });

  // Cerrar sidebar al hacer click en un link (mobile)
  document.querySelectorAll('#admin-sidebar a').forEach((link) => {
    link.addEventListener('click', () => {
      if (window.innerWidth < 1024) {
        closeSidebar();
      }
    });
  });
</script>

<style>
  /* Asegurar que el contenido no se oculte detrás del sidebar */
  @media (min-width: 1024px) {
    .lg\:pl-64 {
      padding-left: 16rem;
    }
  }
</style>

